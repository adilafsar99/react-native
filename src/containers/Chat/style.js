import {makeStyles} from "@mui/styles";

const useStyles = makeStyles(() => ({
  container: {
    height: "100vh",
    backgroundColor: "#e57970"
  },
  messageDiv: {
    backgroundColor: "#df4830",
    padding: "8px"
  },
}));

export {useStyles};